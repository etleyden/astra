# Dockerfile.ui
FROM node:20

WORKDIR /app

# Install dependencies (only once unless package.json changes)
COPY packages/ui/package*.json ./
RUN npm install

# Copy the source code (volume will override this during dev)
COPY packages/ui .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]
